package pageObjects;

import constants.ProductDetailConstants;
import org.openqa.selenium.WebDriver;

public class ProductDetailObjects extends ProductDetailConstants {
    public ProductDetailObjects(WebDriver driver) {
        super(driver);
    }
}
